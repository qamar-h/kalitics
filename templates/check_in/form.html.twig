{% extends 'base.html.twig' %}

{% block title %}Nouveau pointage{% endblock %}

{% block body %}
<main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">{{ check_in.id == null ? "Nouveau pointage" : "Pointage N°: " ~  check_in.id  }}</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
            <div class="btn-group me-2">
                <a href="{{ path('check_in_index') }}" class="btn btn-sm btn-outline-secondary">Tous les pointages</a>
            </div>
        </div>
    </div>
    <div class="table-responsive">

    {% include 'partials/_form.html.twig' %}

    </div>
</main>

{% endblock %}

{% block javascripts %}

    {{ parent() }}

    {{ encore_entry_script_tags('check-in-form-handle') }}

{% endblock %}
